# NetaBoard

Election predictions, promise tracking, and coalition math for Indian politics —
built like a live scoreboard, not a news feed. Next.js 14 (App Router) + Supabase,
deployable free on Vercel.

Already verified: `npm run build` compiles clean, all 19 pages return 200, and every
API route degrades to a clear "demo mode" message when its keys aren't set yet — so
you can deploy this today and wire up real data at your own pace.

## Design

The interface takes its cues from long-form editorial sites — Guardian's bold
masthead structure, Aeon's calm literary reading surface — rather than a neon
SaaS dashboard: warm paper background, serif headlines and body copy (Source
Serif 4), sans-serif reserved for UI chrome (nav, buttons, labels), and
monospace reserved for data (stats, prices, seat counts) so numbers read as
precise and analytical against the prose around them. Three restrained accent
hues — ochre, deep teal, crimson — replace the original neon amber/mint/red,
and cards use hairline borders instead of glow/shadow. All of this lives in
one file, `app/globals.css`, as CSS custom properties — retint the whole site
by editing the values in `:root`, no component changes needed.

## What's actually working right now

| Feature | Status |
|---|---|
| Election Prediction Hub (`/predictions`) | Live, reads from `predictions` table |
| Crowd Prediction Market (`/market`) | Live, real votes via `/api/vote` |
| Coalition Builder (`/coalition`) | Live, reads from `parties` table |
| Politician / Promise Tracker (`/politicians`) | Live, reads `politicians`+`promises`+`timeline_events` |
| Constituency Dashboard (`/constituencies`) | Live, reads `constituencies` table |
| Political Stock Market (`/stock-market`) | Live display; refreshed by `/api/stock-refresh` (Reddit-based sentiment, see note below) |
| Daily AI Brief (`/brief`) | Live display; generated by `/api/daily-brief` (GNews + Gemini) |
| Ask Politics AI (`/ask`) | Live chat via Gemini |
| AI Fact Check (`/fact-check`) | Live via Gemini, grounded in Google's official Fact Check Tools API when configured, logs to `fact_checks` |
| Election Calendar (`/calendar`) | Live, reads `elections` table |
| Debate Arena (`/debate`) | Live, votes + arguments via API |
| Political IQ Quiz (`/quiz`) | Live, reads `quiz_questions` |
| Geopolitical Risk Meter (`/more`) | Live, reads `geopolitical_risk` |
| Live Heat Map (`/heatmap`) | Working grid view (swap in Leaflet for a real map — see below) |
| Manifesto Comparison (`/manifesto`) | Working, calls Gemini via `/api/ask` |
| Historical Explorer (`/history`) | Static factual timeline — wire to Supabase once you load ECI archives |
| Meme Generator (`/memes`) | Fully working, 100% client-side canvas, nothing uploaded |

Everything reads from Supabase when it's configured, and falls back to realistic
demo data (`lib/fallback.js`) when it isn't — so the whole site is browsable the
moment you deploy it, before you've touched the database.

---

## 1. Set up Supabase (free)

1. Create a project at [supabase.com](https://supabase.com) — free tier.
2. Go to **SQL Editor** → paste in `supabase/schema.sql` → run it.
3. Paste in `supabase/seed.sql` → run it. This loads the Bihar 2026 example data
   (real 2020 assembly seat counts, sample politicians/promises, etc).
4. Go to **Project Settings → API** and copy:
   - `Project URL` → `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role` key → `SUPABASE_SERVICE_ROLE_KEY` (**server-only, never expose this**)

## 2. Get free API keys

Configure as many as you want — every gateway (`lib/ai.js`, `lib/news.js`,
`lib/social.js`) is built to use whatever you've set and skip whatever you
haven't, no code changes required either way.

### AI providers (`lib/ai.js` tries these in order, falls through automatically)
| Provider | Get a key | Why it's here |
|---|---|---|
| **Groq** | [console.groq.com/keys](https://console.groq.com/keys) | Free, no card, fastest inference — first choice for chat/fact-check. Uses `openai/gpt-oss-120b` |
| **Gemini** | [aistudio.google.com/apikey](https://aistudio.google.com/apikey) | Free tier, strong general quality — second choice. Uses `gemini-2.5-flash` |
| **OpenRouter** | [openrouter.ai/keys](https://openrouter.ai/keys) | Aggregates many `:free`-suffixed open models — last-resort fallback |

If Groq is rate-limited or down, the code automatically retries with Gemini,
then OpenRouter — that's the "rotation" at work: redundancy across providers,
not a way to dodge any single provider's quota.

**A note on model names**: AI providers deprecate and rename models on a
rolling basis — Groq retired `llama-3.3-70b-versatile` on Aug 16, 2026, and
Gemini retired `gemini-2.0-flash` on Jun 1, 2026, both since this project
started. If Ask AI/Fact Check/Daily Brief suddenly go quiet, check the
`error` field in `/api/ask`'s response first — it names the exact provider
and status code — before assuming your keys are wrong.

### Fact-checking (`lib/factcheck.js` — grounds the AI in real sources)
| Source | Get a key | What it adds |
|---|---|---|
| **Google Fact Check Tools API** | [console.cloud.google.com](https://console.cloud.google.com) → enable "Fact Check Tools API" → Credentials → API key | Free, official. Searches the same ClaimReview database used by PolitiFact, Snopes, BOOM Live, Alt News, and hundreds of other publishers — checked *before* asking the AI, so a claim someone's already fact-checked shows a real cited source instead of a pure model guess |

### News sources (`lib/news.js` — daily brief pulls from every one configured, deduped)
| Source | Get a key | Free tier |
|---|---|---|
| **GNews** | [gnews.io](https://gnews.io) | ~100 requests/day |
| **NewsData.io** | [newsdata.io](https://newsdata.io) | ~200 requests/day |
| **Currents API** | [currentsapi.services](https://currentsapi.services) | ~600 requests/day |
| **The Guardian Open Platform** | [open-platform.theguardian.com](https://open-platform.theguardian.com) | Free, generous — no hard daily cap for developer tier |

### Social / mention-volume sources (`lib/social.js` — drives the Political Stock Market)
| Source | Get access | Notes |
|---|---|---|
| **Wikipedia Pageviews** | Nothing — no key, ever | Zero setup, always on. Page traffic is a solid "how much attention right now" signal |
| **GDELT Project** | Nothing — no key, ever | Free global news-event database, always on |
| **Hacker News** | Nothing — no key, ever | Algolia's free HN search — low volume for Indian politics, but zero cost |
| **Mastodon** | Nothing — just set `MASTODON_INSTANCE` | Public hashtag timelines are open on most instances |
| **Bluesky** | Free account + App Password (bsky.app → Settings → App Passwords) | Approval is instant. Public feed reads need no auth at all; keyword search needs this login |
| **Reddit** | Free OAuth "script" app at [reddit.com/prefs/apps](https://www.reddit.com/prefs/apps) — *optional* | The free tier is real, but as of 2026 new developer apps need manual approval, 2-4 weeks. Not worth waiting on — everything above already covers this feature well without it |
| **X (Twitter)** | Optional, paid only | See note below |

### A note on X (Twitter)
As of **February 2026, X moved to pay-per-use pricing** — no subscriptions
for new developers, no free tier. You buy credits and pay per request:
roughly **$0.005 per post read**, capped at 2M reads/month self-serve. For
this app's scale — a handful of politicians checked once a day via
`/api/stock-refresh` — that's realistically **$15–40/month**, not the old
$200/month "Basic" tier people remember.

`fromX()` in `lib/social.js` is fully wired: create a Project + App at
[developer.x.com](https://developer.x.com), generate a **Bearer Token**, add
billing/credits in the developer portal, then set `X_BEARER_TOKEN` — it
activates automatically, no code changes needed. It uses the standard
`/2/tweets/search/recent` endpoint (not the counts endpoint, whose
pay-per-use availability is inconsistently documented) with `max_results=25`
per call to keep cost predictable.

Until/unless you add it, **Wikipedia Pageviews + GDELT already give a
genuinely free, always-on, zero-signup trending signal** — no setup needed.

### On "multiple keys" and rotation
`lib/keyRotation.js` supports comma-separating multiple keys for one provider
(`GROQ_API_KEY=key1,key2`) and picks one at random per request — useful if you
legitimately hold more than one key (e.g. a personal key and a team key).
Worth being upfront: most providers' free tiers are tied to your *account*,
not the key itself, and their terms of service (Groq's explicitly) prohibit
creating multiple accounts purely to stack free quota. The rotation that
actually matters here is the one across *different providers* in `lib/ai.js`
— that's genuine redundancy, not a workaround.

## 3. Configure environment variables

Copy `.env.example` to `.env.local` for local dev, and add the same variables in
**Vercel → Project → Settings → Environment Variables** for production.

See `.env.example` for the full, commented list (Supabase, three AI providers,
four news sources, four social sources, plus `CRON_SECRET`). None are required
for the site to run — set only what you have, in any combination.

## 4. Deploy to Vercel (free)

```bash
npm install -g vercel
vercel login
vercel          # first deploy, follow the prompts
vercel --prod   # promote to production
```

Or push this folder to a GitHub repo and click **Import Project** on
[vercel.com/new](https://vercel.com/new) — Vercel auto-detects Next.js, no config needed.

`vercel.json` already defines two scheduled jobs:
- `/api/daily-brief` — once a day at 01:30 UTC
- `/api/stock-refresh` — every 6 hours

Vercel automatically sends `Authorization: Bearer $CRON_SECRET` on these requests,
which is what the routes check for — just make sure `CRON_SECRET` is set in your
Vercel project env vars. (Cron jobs run on Vercel's Hobby plan too, just capped to
once a day per job on Hobby — upgrade to Pro if you want the 6-hourly stock refresh.)

## 5. Local development

```bash
npm install
npm run dev
```

Visit `http://localhost:3000`. Without any env vars set, every page still renders
with realistic demo data — good for checking the design before wiring up Supabase.

---

## Project structure

```
app/
  page.js                 → home
  predictions/             → Election Prediction Hub
  market/                  → Crowd Prediction Market
  coalition/                → Coalition Builder
  politicians/[slug]/       → Promise Tracker + Political Timeline
  constituencies/            → Constituency Dashboard
  stock-market/               → Political Stock Market
  brief/                       → Daily AI Brief
  ask/                          → Ask Politics AI chat
  fact-check/                    → AI Fact Check
  calendar/, heatmap/, debate/, quiz/, manifesto/, history/, memes/, more/
  api/                            → route handlers (ask, fact-check, vote, daily-brief, stock-refresh, debate)
lib/
  supabaseClient.js   → browser client (anon key)
  supabaseServer.js   → server client (service role key — server only)
  data.js             → all server-side data fetchers, with fallbacks
  fallback.js         → static demo data
  ai.js               → unified AI gateway — Groq → Gemini → OpenRouter fallback
  news.js             → multi-source news aggregator — GNews, NewsData, Currents, Guardian
  social.js           → mention-volume aggregator — Reddit, Bluesky, Mastodon, HN, (X if paid)
  keyRotation.js       → picks a key at random from a comma-separated env var
components/           → shared UI (Gauge, VoteWidget, CoalitionBuilder, etc.)
supabase/
  schema.sql          → full DB schema + RLS policies
  seed.sql            → demo data
```

## Extending it further

- **Real geographic heat map**: swap `/app/heatmap/page.js`'s grid for
  [react-leaflet](https://react-leaflet.js.org/) + OpenStreetMap tiles, plotting
  each constituency by lat/long instead of a CSS grid.
- **Real leaderboard scoring**: once an election result is entered, write a
  Supabase function that compares `market_votes` against the outcome and updates
  `predictors.accuracy_pct`.
- **Auth**: add Supabase Auth (`@supabase/auth-helpers-nextjs`) if you want the
  leaderboard tied to real accounts instead of anonymous fingerprints.
- **Legal note**: several Indian states restrict publishing opinion/exit poll
  data during specific pre-election windows (Section 126A, Representation of the
  People Act). Check current Election Commission guidance before your prediction
  features go live around an actual election date.

## License / disclaimer

This is a demo scaffold, not a certified forecasting product. Predictions,
"stock" prices, and risk scores are illustrative unless you've connected real
data sources — label them clearly as analysis, not fact, in your own deployment.
